// import { selectDomainRegistrationDialogDomain } from '../selectors';

describe('selectDomainRegistrationDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
