// import { selectChangePasswordModalDomain } from '../selectors';

describe('selectChangePasswordModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
