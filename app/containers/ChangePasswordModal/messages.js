/*
 * ChangePasswordModal Messages
 *
 * This contains all the text for the ChangePasswordModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChangePasswordModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChangePasswordModal container!',
  },
});
