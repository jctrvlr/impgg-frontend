/*
 * TableItemDialog Messages
 *
 * This contains all the text for the TableItemDialog container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableItemDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableItemDialog container!',
  },
});
