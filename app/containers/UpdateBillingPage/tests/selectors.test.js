// import { selectUpdateBillingPageDomain } from '../selectors';

describe('selectUpdateBillingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
