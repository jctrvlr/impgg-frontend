// import { selectDeleteAccountModalDomain } from '../selectors';

describe('selectDeleteAccountModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
