/*
 * DeleteAccountModal Messages
 *
 * This contains all the text for the DeleteAccountModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DeleteAccountModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeleteAccountModal container!',
  },
});
