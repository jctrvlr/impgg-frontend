/*
 * LinkCreationDialog Messages
 *
 * This contains all the text for the LinkCreationDialog container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LinkCreationDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LinkCreationDialog container!',
  },
});
