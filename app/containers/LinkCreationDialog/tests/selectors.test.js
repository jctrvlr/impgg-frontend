// import { selectLinkCreationDialogDomain } from '../selectors';

describe('selectLinkCreationDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
