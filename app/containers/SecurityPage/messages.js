/*
 * SecurityPage Messages
 *
 * This contains all the text for the SecurityPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SecurityPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SecurityPage container!',
  },
});
