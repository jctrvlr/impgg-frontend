// import { selectSecurityPageDomain } from '../selectors';

describe('selectSecurityPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
