/*
 * CheckoutFormPage Messages
 *
 * This contains all the text for the CheckoutFormPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CheckoutFormPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheckoutFormPage container!',
  },
});
