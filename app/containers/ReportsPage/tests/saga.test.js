/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
/*
import { takeLatest, call, put, select } from 'redux-saga/effects';
import reportsPageSaga, { getClickReport } from '../saga';

const generator = reportsPageSaga();

describe('reportsPageSaga Saga', () => {
  it('should wait for every GET_CLICK_REPORT action and call getClickReport', () => {
    expect(generator.next().value).toEqual(
      takeLatest('GET_CLICK_REPORT', getClickReport),
    );
  });

  it('should be done on next iteration', () => {
    expect(generator.next().done).toBeTruthy();
  });

  it('should call api and dispatch success action', async () => {
    const dummyResponse = { status: 200 };
    const requestReports = jest.spyOn()
  });
});
*/
