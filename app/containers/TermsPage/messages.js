/*
 * TermsPage Messages
 *
 * This contains all the text for the TermsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TermsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TermsPage container!',
  },
});
