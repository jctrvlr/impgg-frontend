// import { selectOAuthCallbackDomain } from '../selectors';

describe('selectOAuthCallbackDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
