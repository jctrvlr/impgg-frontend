/*
 * OAuthCallback Messages
 *
 * This contains all the text for the OAuthCallback container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OAuthCallback';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OAuthCallback container!',
  },
});
